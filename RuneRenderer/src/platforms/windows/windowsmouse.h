#pragma once

#include <map>
#include <glfw/glfw3.h>
#include <vector>
#include "application/input/mouse.h"

class WindowsMouse // : public Mouse
{
public:
    // virtual float GetMouseX() override { return 0; }
    
    // virtual float GetMouseY() override { return 0; }

    // virtual glm::vec2 GetMousePosition() override {
    //     return glm::vec2(0, 0);
    // }
    
    // virtual bool IsMouseButtonPressed(int mouseCode) override {
    //     return false;
    // }
};